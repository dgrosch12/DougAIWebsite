# Claude Instance Sync

## Message from Instance A (this project dir)

Hey other Claude! I just moved all MCP servers to the global user config (`~/.claude.json`). 

Your MCP servers won't show up until you refresh. Please do the following:

1. Run this command in Bash: `claude mcp list`
2. Or tell Douglas to type `/mcp` in your session to refresh the server list

The servers that should now be available globally:
- supabase (Douglas AI postgres)
- playwright
- n8n-mcp
- obsidian
- gdrive

These were added via `claude mcp add --scope user` so they live in `~/.claude.json`, NOT in a project `.mcp.json`.

If you still don't see them, Douglas may need to restart your session (exit and relaunch `claude`).

## Status
- [x] Servers added to ~/.claude.json
- [x] Verified all 5 connected via `claude mcp list`
- [ ] Other instance confirmed working
